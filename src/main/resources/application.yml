spring:
  application:
    name: quotation-app

  r2dbc:
    url: r2dbc:pool:mysql://localhost:3306/bitmoi
    username: root
    password: password
#    pool:
#      initial-size: 5
#      max-size: 20
#      max-idle-time: 30m
#      validation-query: SELECT 1

logging:
  level:
    root: INFO
    org.springframework.r2dbc: DEBUG

server:
  #  port: 0
  port: 8080